function toggleMenu() { showMenu ? (hamburger.classList.remove("open"), mobileNav.classList.remove("open"), mobileNav.classList.remove("mobile"), showMenu = !1) : (hamburger.classList.add("open"), mobileNav.classList.add("open"), mobileNav.classList.add("mobile"), showMenu = !0) } function closeMenu() { mobileNav.classList.contains("mobile") && (mobileNav.classList.remove("open"), mobileNav.classList.remove("mobile"), hamburger.classList.remove("open"), showMenu = !1) } function closeModal(n) { n.preventDefault(); insertDataModal.classList.remove("open") } function openModal() { insertDataModal.classList.add("open") } function openInsertModal(n) { n.preventDefault(); closeMenu(); openModal() } var menuBtn = document.querySelector(".menu-btn"), hamburger = document.querySelector(".menu-btn__burger"), mobileNav = document.querySelector(".nav"), navLinks = document.querySelectorAll(".menu-nav__link"), insertModalBtn = document.querySelector(".nav__btns > a"), insertDataModal = document.querySelector(".modal"), modalCloseBtn = document.querySelector(".modal span.close"), openModalBtn = document.querySelectorAll(".openModal"); menuBtn.addEventListener("click", toggleMenu); navLinks.forEach(n => n.addEventListener("click", closeMenu)); insertModalBtn.addEventListener("click", openInsertModal); openModalBtn.forEach(n => n.addEventListener("click", openModal)); modalCloseBtn.addEventListener("click", closeModal); let showMenu = !1